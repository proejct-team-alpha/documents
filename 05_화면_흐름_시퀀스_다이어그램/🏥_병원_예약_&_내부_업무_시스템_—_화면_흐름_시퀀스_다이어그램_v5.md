# 🏥 병원 예약 & 내부 업무 시스템 — 화면 흐름 시퀀스 다이어그램

> **문서 버전:** v5.0 (스토리보드 정합 기반)
> **작성일:** 2026년
> **변경 이력:** v2.0 → v3.0 — 전 ROLE 대시보드 추가 / SSR PRG 패턴 반영 / 방문 접수 흐름 추가 / 챗봇 이력 저장 반영 / URL 전면 정정
> v3.0 → v4.0 — ERD v3.0 정합: CHATBOT_HISTORY 테이블 반영, RESERVATION.source 컬럼 반영, API 명세서 v4.0 연동
> v4.0 → v5.0 — 스토리보드 정합 — 내 정보관리 흐름 추가, 예약 상태 전이 권한 스토리보드 기준 통일, API 명세서 v5.0·ERD v4.0 동기화
> **연관 문서:** 프로젝트 계획서 v4.2 / ERD v4.0 / 화면 정의서 v2.0 / API 명세서 v5.0
> **기준:** Spring Boot SSR (Mustache) + PRG (Post-Redirect-Get) 패턴 + RPC 스타일 URL

---

## 목차

01. [외부 사용자 — LLM 증상 추천 예약 흐름](#1-외부-사용자--llm-증상-추천-예약-흐름)
02. [외부 사용자 — 직접 선택 예약 흐름](#2-외부-사용자--직접-선택-예약-흐름)
03. [접수 직원 — 로그인·대시보드·접수 처리 흐름](#3-접수-직원--로그인대시보드접수-처리-흐름-role_staff)
04. [접수 직원 — 방문 접수 흐름](#4-접수-직원--방문-접수-흐름-role_staff)
05. [의사 — 로그인·대시보드·진료 처리 흐름](#5-의사--로그인대시보드진료-처리-흐름-role_doctor)
06. [간호사 — 로그인·대시보드·업무 조회 흐름](#6-간호사--로그인대시보드업무-조회-흐름-role_nurse)
07. [의사·간호사 — 병원 규칙 Q&A 챗봇 흐름](#7-의사간호사--병원-규칙-qa-챗봇-흐름)
08. [관리자 — 로그인·대시보드·예약 관리 흐름](#8-관리자--로그인대시보드예약-관리-흐름-role_admin)
09. [관리자 — 병원 규칙 관리 흐름](#9-관리자--병원-규칙-관리-흐름-role_admin)
10. [관리자 — 인사 관리 흐름](#10-관리자--인사-관리-흐름-role_admin)
11. [물품 관리자 — 물품 관리 흐름](#11-물품-관리자--물품-관리-흐름-role_item_manager)
12. [내 정보관리 흐름 (전 역할)](#12-내-정보관리-흐름-전-역할)
13. [전체 상태 흐름 다이어그램](#13-전체-상태-흐름-다이어그램)
14. [권한 기반 화면 흐름 요약](#14-권한-기반-화면-흐름-요약)
15. [SSR PRG 패턴 상세](#15-ssr-prg-패턴-상세)
16. [LLM 연동 흐름 상세](#16-llm-연동-흐름-상세)
17. [v4.0 → v5.0 변경 내역](#17-v40--v50-변경-내역)

---

## 1. 외부 사용자 — LLM 증상 추천 예약 흐름

### 🎯 목적
비회원 환자가 증상 텍스트를 입력하면 LLM이 진료과·의사·시간을 추천하고, 환자가 확인 후 예약을 확정한다.

### 📌 주요 상태 변화
| 단계 | 상태 |
|------|------|
| 예약 확정 시 | `RESERVED` 생성 |
| 중복 예약 시 | 오류 — 폼 재렌더링, 저장 없음 |
| LLM API 실패 시 | 직접 선택 화면으로 폴백 |

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant P  as Patient (외부)
    participant W  as Web Server
    participant L  as LlmService
    participant C  as Claude API
    participant DB as Database

    P->>W: GET /
    W->>DB: 활성 진료과 목록 조회 (is_active = TRUE)
    DB-->>W: 진료과 목록 반환
    W-->>P: 비회원 메인 화면 렌더링 (index)

    P->>W: GET /reservation
    W-->>P: 예약 방식 선택 화면 렌더링 (reservation/index)
    Note over P: "AI 추천 받기" 선택

    P->>W: GET /reservation/symptom
    W-->>P: 증상 입력 폼 렌더링 (reservation/symptom)

    P->>W: POST /llm/symptom/analyze  [AJAX]
    Note over P,W: {symptomText: "..."}
    W->>L: analyzeSymptom(symptomText)

    L->>DB: 활성 진료과 목록 조회
    DB-->>L: 진료과 목록 반환
    L->>DB: 의사 목록 조회 (specialty, available_days)
    DB-->>L: 의사 목록 반환
    L->>C: 시스템 프롬프트(진료과·의사 주입) + 증상 텍스트
    Note over L,C: model: claude-sonnet-4-6 / timeout: 5s

    alt LLM 응답 성공
        C-->>L: 추천 결과 JSON 반환
        L->>DB: LlmRecommendation 저장 (is_used = FALSE)
        DB-->>L: recommendationId 반환
        L-->>W: RecommendationDto 반환
        W-->>P: JSON 응답 {success:true, data:{recommendationId, dept, doctor, time, summary, disclaimer}}
        Note over P: 추천 결과 + 면책 고지 화면 표시

        P->>W: GET /reservation/direct?recommendedDept=외과&recommendedDoctor=김철수&...
        W->>DB: 활성 진료과 목록 조회
        DB-->>W: 진료과 목록 반환
        W-->>P: 예약 폼 렌더링 (reservation/direct) — 추천값 자동 입력

        P->>W: POST /reservation/create
        Note over P,W: {patientName, phone, birthDate, gender, departmentId, doctorId, date, timeSlot, llmRecommendationId}
        W->>DB: 중복 예약 체크 (doctor_id + date + time_slot UNIQUE)
        DB-->>W: 가능 여부 반환

        alt 예약 가능
            W->>DB: Patient 저장 (upsert)
            W->>DB: Reservation 저장 (status=RESERVED, source=ONLINE)
            W->>DB: LlmRecommendation.is_used = TRUE 업데이트
            W-->>P: redirect:/reservation/complete [PRG]
            Note over W,P: Flash: successMessage, reservationNumber
            P->>W: GET /reservation/complete
            W-->>P: 예약 완료 화면 렌더링 (reservation/complete)
        else 중복 발생
            W-->>P: 폼 재렌더링 (reservation/direct)
            Note over W,P: request.setAttribute errorCode=DUPLICATE_RESERVATION / inputData 복원
        end

    else LLM API 실패 (타임아웃 5초 초과 등)
        C-->>L: 오류 반환
        L-->>W: 폴백 신호
        W-->>P: JSON {success:false, errorCode:LLM_SERVICE_UNAVAILABLE}
        Note over P: "직접 선택하기" 버튼 노출 → /reservation/direct 이동
    end
```

### 📝 흐름 설명

1. **비회원 메인** : `/`에서 활성 진료과 안내 → 예약 방식 선택 화면으로 이동.
2. **LLM 호출** : AJAX POST → JSON 응답. `LlmService`가 DB에서 진료과·의사를 조회한 뒤 Claude API 프롬프트에 주입.
3. **추천 결과** : `recommendationId` 포함 응답. 예약 확정 시 `llmRecommendationId`로 전달 → `is_used = TRUE` 업데이트.
4. **예약 확정** : PRG 패턴. `POST /reservation/create` 성공 시 `redirect:/reservation/complete` + Flash Attribute. Reservation 저장 시 `source=ONLINE` 설정.
5. **오류 처리** : 중복 예약 시 `reservation/direct` 폼 재렌더링 + 입력값 복원.
6. **면책 고지** : 추천 결과 화면에 `disclaimer` 문구 필수 표시.

---

## 2. 외부 사용자 — 직접 선택 예약 흐름

### 🎯 목적
LLM 추천 없이 환자가 진료과·의사·시간을 직접 선택하여 예약한다. AI 추천 흐름의 폴백 경로이기도 하다.

### 📌 주요 상태 변화
| 단계 | 상태 |
|------|------|
| 예약 정보 제출 후 저장 | `RESERVED` 생성 |
| 중복 예약 시 | 오류 — 폼 재렌더링, 저장 없음 |

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant P  as Patient (외부)
    participant W  as Web Server
    participant DB as Database

    P->>W: GET /reservation/direct
    W->>DB: 활성 진료과 목록 조회
    DB-->>W: 진료과 목록 반환
    W-->>P: 예약 폼 렌더링 (reservation/direct) — departments 모델 주입

    P->>W: GET /reservation/getDoctors?departmentId=2  [AJAX]
    W->>DB: 해당 진료과 의사 목록 조회 (available_days 포함)
    DB-->>W: 의사 목록 반환
    W-->>P: JSON {success:true, data:{doctors:[...]}}
    Note over P: 의사 선택 드롭다운 동적 갱신

    P->>W: GET /reservation/getSlots?doctorId=1&date=2026-03-15  [AJAX]
    W->>DB: 해당 의사·날짜 기준 예약된 슬롯 조회
    DB-->>W: 예약된 슬롯 반환
    W-->>P: JSON {success:true, data:{availableSlots:[...], unavailableSlots:[...]}}
    Note over P: 시간 선택 버튼 동적 갱신

    P->>W: POST /reservation/create
    Note over P,W: Form: patientName, phone, birthDate, gender, departmentId, doctorId, date, timeSlot
    W->>DB: 중복 예약 체크 (doctor_id + date + time_slot UNIQUE)
    DB-->>W: 가능 여부 반환

    alt 예약 가능
        W->>DB: Patient 저장 (phone 기준 upsert)
        W->>DB: Reservation 저장 (status=RESERVED, source=ONLINE)
        W-->>P: redirect:/reservation/complete [PRG]
        Note over W,P: Flash: successMessage, reservationNumber
        P->>W: GET /reservation/complete
        W-->>P: 예약 완료 화면 렌더링 (reservation/complete)
    else 중복 발생
        W-->>P: 폼 재렌더링 (reservation/direct)
        Note over W,P: request.setAttribute: errorCode / errorMessage / inputData
    end
```

### 📝 흐름 설명

1. **진료과 선택** : `is_active = TRUE`인 진료과 목록만 표시.
2. **의사 조회 (AJAX)** : `GET /reservation/getDoctors` — 진료과 선택 시 동적 갱신.
3. **시간 슬롯 조회 (AJAX)** : `GET /reservation/getSlots` — 의사·날짜 선택 시 동적 갱신. 이미 예약된 슬롯 비활성화.
4. **예약 생성** : PRG 패턴. 성공 → redirect + Flash. 실패 → 폼 재렌더링 + `inputData` 복원. Reservation 저장 시 `source=ONLINE` 설정.

---

## 3. 접수 직원 — 로그인·대시보드·접수 처리 흐름 (ROLE_STAFF)

### 🎯 목적
예약된 환자가 실제 방문하면 접수 처리하여 상태를 `RECEIVED`로 변경하고, 추가 정보를 입력한다.

### 📌 주요 상태 변화
| 이전 상태 | 이후 상태 | 처리 주체 |
|-----------|-----------|-----------|
| `RESERVED` | `RECEIVED` | `ROLE_STAFF` |

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant S  as Staff (ROLE_STAFF)
    participant W  as Web Server
    participant DB as Database

    S->>W: POST /login (username, password)
    W->>DB: Staff 인증 (BCrypt 비밀번호 검증)
    DB-->>W: ROLE_STAFF 세션 발급
    W-->>S: redirect:/staff/dashboard [Spring Security]

    S->>W: GET /staff/dashboard
    W->>DB: 오늘 예약 집계 쿼리 (totalCount, reservedCount, receivedCount)
    W->>DB: 미접수 상위 5건 조회 (status=RESERVED ORDER BY time_slot)
    DB-->>W: 집계 + 상위 5건 반환
    W-->>S: 대시보드 렌더링 (staff/dashboard)
    Note over S: staffName, today, 집계, upcomingReservations 표시

    S->>W: GET /staff/reception/list
    W->>DB: 오늘 RESERVED 예약 목록 조회 (reservation_date=TODAY)
    DB-->>W: 예약 목록 반환
    W-->>S: 접수 목록 화면 렌더링 (staff/reception/list)

    S->>W: GET /staff/reception/detail?reservationId=10
    W->>DB: Reservation 조회 (reservationId=10)
    W->>DB: Patient 조회
    DB-->>W: 예약 + 환자 정보 반환
    W-->>S: 접수 처리 폼 렌더링 (staff/reception/detail)
    Note over S: 주소, 특이사항 입력 가능

    S->>W: POST /staff/reception/receive
    Note over S,W: {reservationId:10, address:"부산시...", note:"고혈압..."}
    W->>DB: Reservation 상태 검증 (status = RESERVED 확인)
    DB-->>W: 검증 통과

    alt 정상 처리
        W->>DB: Patient.address, note 업데이트
        W->>DB: Reservation.status = RECEIVED 변경
        W-->>S: redirect:/staff/reception/list [PRG]
        Note over W,S: Flash: successMessage = "접수가 완료되었습니다."
        S->>W: GET /staff/reception/list
        W-->>S: 목록 화면 렌더링 (successMessage 표시)
    else 상태 오류 (이미 접수·취소됨)
        W-->>S: 폼 재렌더링 (staff/reception/detail)
        Note over W,S: request.setAttribute: errorCode=INVALID_STATUS_TRANSITION / errorMessage
    end
```

### 📝 흐름 설명

1. **로그인 → 대시보드** : 로그인 성공 시 `/staff/dashboard` 자동 이동. 오늘 예약 현황 한눈에 파악.
2. **접수 목록** : 당일 `RESERVED` 상태만 필터링. 이미 접수된 환자는 구분 표시.
3. **접수 처리** : 주소·특이사항 입력 후 `RECEIVED` 상태 변경. PRG 패턴으로 중복 제출 방지.
4. **오류 처리** : 상태 검증 실패 시 폼 재렌더링. 다른 직원이 먼저 처리한 경우 포함.

### 📋 사이드바 메뉴 (ROLE_STAFF)

```
📊 대시보드         → /staff/dashboard
📋 접수 목록        → /staff/reception/list
📞 전화 예약 등록    → /staff/reservation/new
🚶 방문 접수        → /staff/walkin/new
👤 내 정보관리       → /staff/mypage
```

---

## 3-1. 접수 직원 — 전화 예약 등록 흐름 (ROLE_STAFF)

### 🎯 목적
환자가 전화로 예약을 요청하면 접수 직원이 대신 예약을 등록한다.

### 📌 주요 상태 변화
| 단계 | 상태 | 특이사항 |
|------|------|----------|
| 전화 예약 등록 | `RESERVED` 생성 | source=PHONE |

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant S  as Staff (ROLE_STAFF)
    participant W  as Web Server
    participant DB as Database

    S->>W: GET /staff/reservation/new
    W->>DB: 활성 진료과 목록 조회
    DB-->>W: 진료과 목록 반환
    W-->>S: 전화 예약 등록 폼 렌더링 (staff/reservation/new)

    S->>W: GET /reservation/getDoctors?departmentId=1  [AJAX]
    W->>DB: 해당 진료과 의사 목록 조회
    DB-->>W: 의사 목록 반환
    W-->>S: JSON {success:true, data:{doctors:[...]}}

    S->>W: POST /staff/reservation/create
    Note over S,W: {patientName, phone, birthDate, gender, departmentId, doctorId, reservationDate, timeSlot}
    W->>DB: 슬롯 중복 체크 (doctor_id + date + time_slot)
    DB-->>W: 가능 여부 반환

    alt 등록 가능
        W->>DB: Patient 저장 (phone 기준 upsert)
        W->>DB: Reservation 저장 (status=RESERVED, source=PHONE)
        W-->>S: redirect:/staff/reception/list [PRG]
        Note over W,S: Flash: successMessage = "전화 예약이 등록되었습니다." / reservationNumber
    else 슬롯 중복
        W-->>S: 폼 재렌더링 (staff/reservation/new)
        Note over W,S: request.setAttribute: errorCode=DUPLICATE_RESERVATION / inputData 복원
    end
```

### 📝 흐름 설명

1. **전화 예약 특성** : 접수 직원이 환자 정보를 대신 입력. `source=PHONE`으로 저장하여 온라인 예약과 구분.
2. **예약 상태** : `RESERVED` 상태로 생성. 이후 환자 방문 시 접수 처리 흐름으로 진행.
3. **PRG 패턴** : 등록 성공 시 접수 목록으로 redirect.

---

## 4. 접수 직원 — 방문 접수 흐름 (ROLE_STAFF)

### 🎯 목적
사전 예약 없이 병원에 직접 방문한 환자를 즉시 접수 처리한다. 예약 생성과 접수가 동시에 이루어진다.

### 📌 주요 상태 변화
| 단계 | 상태 | 특이사항 |
|------|------|----------|
| 방문 접수 등록 즉시 | `RECEIVED` 직행 | `RESERVED` 단계 없음 |
| source 구분 | `WALKIN` | 온라인(`ONLINE`), 전화(`PHONE`)와 구분 |

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant S  as Staff (ROLE_STAFF)
    participant W  as Web Server
    participant DB as Database

    S->>W: GET /staff/walkin/new
    W->>DB: 활성 진료과 목록 조회
    DB-->>W: 진료과 목록 반환
    W-->>S: 방문 접수 폼 렌더링 (staff/walkin/new)
    Note over S: 오늘 날짜 기본값, 진료과·의사 선택

    S->>W: GET /reservation/getDoctors?departmentId=1  [AJAX]
    W->>DB: 해당 진료과 의사 목록 조회
    DB-->>W: 의사 목록 반환
    W-->>S: JSON {success:true, data:{doctors:[...]}}

    S->>W: POST /staff/walkin/create
    Note over S,W: {patientName, phone, birthDate, gender, address, note, departmentId, doctorId, reservationDate, timeSlot}
    W->>DB: 슬롯 중복 체크 (doctor_id + date + time_slot)
    DB-->>W: 가능 여부 반환

    alt 등록 가능
        Note over W,DB: @Transactional 시작
        W->>DB: Patient 저장 (phone 기준 upsert — 기존 환자 재방문 고려)
        W->>DB: Reservation 저장 (status=RECEIVED, source=WALKIN)
        Note over W,DB: @Transactional 커밋
        W-->>S: redirect:/staff/reception/list [PRG]
        Note over W,S: Flash: successMessage = "방문 접수가 완료되었습니다." / reservationNumber
        S->>W: GET /staff/reception/list
        W-->>S: 목록 화면 렌더링
    else 슬롯 중복
        W-->>S: 폼 재렌더링 (staff/walkin/new)
        Note over W,S: request.setAttribute: errorCode=DUPLICATE_RESERVATION / inputData 복원 / departments
    else 의사 진료 불가 (available_days 불일치)
        W-->>S: 폼 재렌더링 (staff/walkin/new)
        Note over W,S: request.setAttribute: errorCode=DOCTOR_NOT_AVAILABLE
    end
```

### 📝 흐름 설명

1. **방문 접수 특성** : 사전 예약 없이 `RECEIVED` 상태로 직행. 접수 처리 단계 생략.
2. **트랜잭션** : `Patient` 저장과 `Reservation` 저장을 `@Transactional`로 묶음. 중간 실패 시 전체 롤백.
3. **환자 중복 처리** : 같은 연락처 환자가 재방문하는 경우 기존 `Patient` 레코드를 재사용 (upsert).
4. **source 구분** : `source = WALKIN`으로 저장. 관리자 `/admin/reception/list`에서 예약 유형 구분 표시.

---

## 5. 의사 — 로그인·대시보드·진료 처리 흐름 (ROLE_DOCTOR)

### 🎯 목적
접수 완료된 환자를 진료하고 기록을 작성하여 진료를 완료 처리한다.

### 📌 주요 상태 변화
| 이전 상태 | 이후 상태 | 처리 주체 |
|-----------|-----------|-----------|
| `RECEIVED` | `COMPLETED` | `ROLE_DOCTOR` |

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant D  as Doctor (ROLE_DOCTOR)
    participant W  as Web Server
    participant DB as Database

    D->>W: POST /login (username, password)
    W->>DB: Staff 인증 (ROLE_DOCTOR 확인)
    DB-->>W: ROLE_DOCTOR 세션 발급
    W-->>D: redirect:/doctor/dashboard [Spring Security]

    D->>W: GET /doctor/dashboard
    W->>DB: 오늘 진료 집계 쿼리 (doctor_id = 본인, totalCount, waitingCount, completedCount)
    W->>DB: 진료 대기 상위 3건 조회 (status=RECEIVED ORDER BY time_slot)
    DB-->>W: 집계 + 상위 3건 반환
    W-->>D: 대시보드 렌더링 (doctor/dashboard)
    Note over D: doctorName, departmentName, 집계, upcomingTreatments 표시

    D->>W: GET /doctor/treatment/list
    W->>DB: Reservation 조회 (status=RECEIVED AND doctor_id=본인, reservation_date=TODAY)
    DB-->>W: 본인 담당 환자 목록 반환
    W-->>D: 진료 목록 화면 렌더링 (doctor/treatment/list)

    D->>W: GET /doctor/treatment/detail?reservationId=10
    W->>DB: Reservation 상세 조회 (본인 담당 환자 여부 검증 포함)
    W->>DB: Patient 기본 정보 조회
    DB-->>W: 예약 + 환자 정보 반환
    W-->>D: 진료 기록 입력 폼 렌더링 (doctor/treatment/detail)

    D->>W: POST /doctor/treatment/complete
    Note over D,W: {reservationId:10, diagnosisNote:"..."}
    W->>DB: Reservation 상태 검증 (status=RECEIVED AND doctor_id=본인)
    DB-->>W: 검증 통과

    alt 정상 처리
        Note over W,DB: @Transactional 시작
        W->>DB: TreatmentRecord 저장 (diagnosis_note, created_at)
        W->>DB: Reservation.status = COMPLETED 변경
        Note over W,DB: @Transactional 커밋 (실패 시 전체 롤백)
        W-->>D: redirect:/doctor/treatment/list [PRG]
        Note over W,D: Flash: successMessage = "진료가 완료 처리되었습니다."
        D->>W: GET /doctor/treatment/list
        W-->>D: 목록 화면 렌더링 (successMessage 표시)
    else 상태 오류
        W-->>D: 폼 재렌더링 (doctor/treatment/detail)
        Note over W,D: request.setAttribute: errorCode=INVALID_STATUS_TRANSITION
    else 타인 환자 접근
        W-->>D: redirect:/doctor/treatment/list [PRG]
        Note over W,D: Flash: errorCode=NOT_OWN_PATIENT
    end
```

### 📝 흐름 설명

1. **로그인 → 대시보드** : 로그인 성공 시 `/doctor/dashboard` 자동 이동. 본인 오늘 진료 집계 표시.
2. **본인 담당 환자 필터링** : `doctor_id = 로그인 의사 ID` 조건. 다른 의사 환자 노출 없음.
3. **트랜잭션 묶음** : `TreatmentRecord` 저장 + `Reservation.status = COMPLETED` 변경은 반드시 `@Transactional` 하나로 처리.
4. **PRG 패턴** : 완료 후 목록 화면으로 리다이렉트. 새로고침 시 중복 POST 방지.

### 📋 사이드바 메뉴 (ROLE_DOCTOR)

```
📊 대시보드         → /doctor/dashboard
💊 진료 목록        → /doctor/treatment/list
💬 병원 규칙 챗봇    → [오버레이]
👤 내 정보관리       → /doctor/mypage
```

---

## 6. 간호사 — 로그인·대시보드·업무 조회 흐름 (ROLE_NURSE)

### 🎯 목적
당일 예약 현황과 접수 상태를 확인하고, 필요 시 환자 기본 정보를 수정한다.

### 📌 주요 상태 변화
> 간호사는 예약 상태 변경 권한 없음. 조회 및 환자 정보 수정만 가능.

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant N  as Nurse (ROLE_NURSE)
    participant W  as Web Server
    participant DB as Database

    N->>W: POST /login (username, password)
    W->>DB: Staff 인증 (ROLE_NURSE 확인)
    DB-->>W: ROLE_NURSE 세션 발급
    W-->>N: redirect:/nurse/dashboard [Spring Security]

    N->>W: GET /nurse/dashboard
    W->>DB: 오늘 예약 상태별 집계 쿼리 (total, reserved, received, completed)
    W->>DB: 진료 대기 상위 3건 조회 (status=RECEIVED ORDER BY time_slot)
    W->>DB: 재고 부족 물품 조회 (quantity < min_quantity)
    DB-->>W: 집계 + 상위 3건 + 재고 부족 반환
    W-->>N: 대시보드 렌더링 (nurse/dashboard)
    Note over N: 오늘 현황 요약 / upcomingReceived / shortageItems 표시

    N->>W: GET /nurse/schedule/list
    W->>DB: 오늘 전체 예약 조회 (모든 상태)
    DB-->>W: 상태별 예약 목록 반환
    W-->>N: 예약 현황 화면 렌더링 (nurse/schedule/list)
    Note over N: RESERVED / RECEIVED / COMPLETED / CANCELLED 구분 표시

    N->>W: GET /nurse/patient/detail?patientId=5
    W->>DB: Patient 정보 조회
    DB-->>W: 환자 정보 반환
    W-->>N: 환자 조회·수정 폼 렌더링 (nurse/patient/detail)

    alt 환자 정보 수정
        N->>W: POST /nurse/patient/update
        Note over N,W: {patientId:5, name:"홍길동", phone:"010-...", note:"당뇨..."}
        W->>DB: Patient 정보 업데이트
        W-->>N: redirect:/nurse/patient/detail?patientId=5 [PRG]
        Note over W,N: Flash: successMessage = "환자 정보가 수정되었습니다."
        N->>W: GET /nurse/patient/detail?patientId=5
        W-->>N: 폼 재렌더링 (successMessage 표시)
    end
```

### 📝 흐름 설명

1. **로그인 → 대시보드** : 로그인 성공 시 `/nurse/dashboard` 자동 이동. 오늘 현황 + 재고 부족 알림 표시.
2. **현황 조회** : 당일 전체 상태별 예약 목록. `ROLE_STAFF`와 달리 모든 상태 조회 (읽기 전용).
3. **환자 정보 수정** : 이름·연락처·생년월일·성별·특이사항 수정 가능. 예약 상태 변경 권한 없음.
4. **PRG 패턴** : 수정 후 동일 상세 화면으로 리다이렉트 + Flash 성공 메시지.

### 📋 사이드바 메뉴 (ROLE_NURSE)

```
📊 대시보드         → /nurse/dashboard
📅 예약 현황        → /nurse/schedule/list
🧑‍⚕️ 환자 정보       → /nurse/patient/detail
💬 병원 규칙 챗봇    → [오버레이]
👤 내 정보관리       → /nurse/mypage
```

---

## 7. 의사·간호사 — 병원 규칙 Q&A 챗봇 흐름

### 🎯 목적
`ROLE_DOCTOR` · `ROLE_NURSE`가 업무 화면에서 병원 규칙, 처리 절차, 물품 위치 등을 질문하면 LLM이 등록된 규칙 문서 기반으로 답변한다. 대화 이력은 세션 단위로 저장된다.

### 📌 LLM 동작 방식
- 관리자가 등록한 `HOSPITAL_RULE` 테이블의 텍스트를 시스템 프롬프트에 주입
- 등록된 규칙 범위 내에서만 답변 / 범위 외 질문은 "확인 불가" 응답
- 대화 이력 저장: `CHATBOT_HISTORY` 테이블에 `session_id`, `staff_id`, `question`, `answer`, `created_at` 컬럼으로 저장

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant U  as Doctor / Nurse
    participant W  as Web Server
    participant L  as LlmService
    participant C  as Claude API
    participant DB as Database

    Note over U: 진료/간호 화면에서 우측 하단 [💬] 챗봇 버튼 클릭

    U->>W: GET /llm/rules/history  [AJAX]
    Note over U,W: 챗봇 패널 열기 시 기존 이력 복원
    W->>DB: CHATBOT_HISTORY 조회 (session_id = 현재 세션)
    DB-->>W: 이력 목록 반환
    W-->>U: JSON {success:true, data:{history:[...]}}
    Note over U: 기존 대화 이력 복원 표시

    U->>W: POST /llm/rules/ask  [AJAX]
    Note over U,W: {question: "응급 처치 키트 위치?"}
    W->>L: askRule(question, sessionId, staffId)

    L->>DB: HOSPITAL_RULE 전체 조회 (is_active = TRUE)
    DB-->>L: 규칙 목록 반환 (title + content)
    Note over L: 규칙 목록을 시스템 프롬프트에 주입

    L->>C: 시스템 프롬프트(규칙 주입) + 질문 텍스트
    Note over L,C: max_tokens=500 / timeout: 5s

    alt 규칙 내 답변 가능
        C-->>L: 규칙 기반 답변 반환
        L->>DB: CHATBOT_HISTORY 저장 (session_id, staff_id, question, answer, created_at)
        L-->>W: 답변 텍스트 반환
        W-->>U: JSON {success:true, data:{answer:"..."}}
    else 규칙 외 질문
        C-->>L: "등록된 규칙에서 확인할 수 없습니다" 반환
        L->>DB: CHATBOT_HISTORY 저장 (session_id, staff_id, question, answer, created_at)
        L-->>W: 안내 메시지 반환
        W-->>U: JSON {success:true, data:{answer:"담당자에게 직접 문의..."}}
    else LLM API 실패
        C-->>L: 오류 반환
        L-->>W: 폴백 신호
        W-->>U: JSON {success:false, errorCode:LLM_SERVICE_UNAVAILABLE}
        Note over U: "서비스 일시 불가, 관리자에게 문의하세요"
    end
```

### 📝 흐름 설명

1. **챗봇 진입** : 화면 우측 하단 [💬] 고정 버튼. 업무 화면 이탈 없이 사용.
2. **이력 복원** : 오버레이 열기 시 `GET /llm/rules/history` AJAX 호출로 기존 대화 이력 복원.
3. **규칙 주입** : 매 요청마다 `is_active = TRUE` 규칙 전체 조회 → 시스템 프롬프트 동적 구성. 관리자 규칙 수정 시 다음 요청부터 즉시 반영.
4. **이력 저장** : 성공·범위 외 답변 모두 `CHATBOT_HISTORY` 테이블에 `session_id`, `staff_id`, `question`, `answer`, `created_at` 컬럼으로 저장. API 실패 시 저장 생략.

---

## 8. 관리자 — 로그인·대시보드·예약 관리 흐름 (ROLE_ADMIN)

### 🎯 목적
전체 예약·접수 현황을 조회하고 상태를 관리(취소 처리 등)한다.

### 📌 주요 상태 변화
| 단계 | 설명 |
|------|------|
| 예약 취소 | `COMPLETED` 제외 모든 상태에서 `CANCELLED` 처리 가능 |

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant A  as Admin (ROLE_ADMIN)
    participant W  as Web Server
    participant DB as Database

    A->>W: POST /login (username, password)
    W->>DB: Staff 인증 (ROLE_ADMIN 확인)
    DB-->>W: ROLE_ADMIN 세션 발급
    W-->>A: redirect:/admin/dashboard [Spring Security]

    A->>W: GET /admin/dashboard
    W->>DB: 통계 쿼리 4종 실행
    Note over W,DB: 오늘 예약 수 / 전체 예약 수 / 진료과별 count / 재고 부족 수
    DB-->>W: 통계 데이터 반환
    W-->>A: 대시보드 렌더링 (admin/dashboard)

    Note over A: 비동기 갱신 시
    A->>W: GET /admin/dashboard/stats  [AJAX]
    W->>DB: 동일 통계 쿼리 실행
    DB-->>W: 통계 반환
    W-->>A: JSON {success:true, data:{...통계}}

    A->>W: GET /admin/reception/list?date=2026-03-15
    W->>DB: 당일 접수 전체 조회 (source별 — ONLINE/PHONE/WALKIN 필터링 가능)
    DB-->>W: 접수 목록 + 상태별 집계 반환
    W-->>A: 전체 접수 목록 렌더링 (admin/reception/list)
    Note over A: source별(ONLINE/PHONE/WALKIN) 필터링 드롭다운 제공

    A->>W: GET /admin/reservation/list?status=RESERVED
    W->>DB: 전체 예약 조회 (날짜·상태·의사·source 필터 적용)
    DB-->>W: 예약 목록 반환
    W-->>A: 전체 예약 목록 렌더링 (admin/reservation/list)

    alt 예약 취소 처리
        A->>W: POST /admin/reservation/cancel
        Note over A,W: {reservationId:10}
        W->>DB: Reservation 상태 검증 (COMPLETED 여부)
        DB-->>W: 검증 결과

        alt 취소 가능 (COMPLETED 아님)
            W->>DB: Reservation.status = CANCELLED 변경
            W-->>A: redirect:/admin/reservation/list [PRG]
            Note over W,A: Flash: successMessage = "예약이 취소되었습니다."
        else 취소 불가 (COMPLETED)
            W-->>A: redirect:/admin/reservation/list [PRG]
            Note over W,A: Flash: errorCode=CANNOT_CANCEL_COMPLETED / errorMessage
        end
        A->>W: GET /admin/reservation/list
        W-->>A: 목록 렌더링 (Flash 메시지 표시)
    end

    A->>W: GET /admin/patient/detail?patientId=5
    W->>DB: Patient 상세 + 전체 예약 이력 조회
    DB-->>W: 환자 + 이력 반환
    W-->>A: 환자 상세 화면 렌더링 (admin/patient/detail)
```

### 📝 흐름 설명

1. **로그인 → 대시보드** : 로그인 성공 시 `/admin/dashboard` 자동 이동.
2. **전체 접수 목록** : `/admin/reception/list` — 날짜·진료과·상태·예약 구분(ONLINE/PHONE/WALKIN) 필터. source별 필터링 드롭다운을 통해 예약 유입 경로 구분 조회 가능.
3. **대시보드 AJAX 갱신** : `GET /admin/dashboard/stats`로 통계 데이터를 비동기 갱신 가능.
4. **취소 PRG** : 취소 성공·실패 모두 목록 화면으로 redirect + Flash Attribute.

### 📋 사이드바 메뉴 (ROLE_ADMIN)

```
📊 대시보드         → /admin/dashboard
📋 접수 현황        → /admin/reception/list
📅 예약 관리        → /admin/reservation/list
🧑‍⚕️ 환자 목록       → /admin/patient/list
👥 직원 관리        → /admin/staff/list
🏥 진료과 관리       → /admin/department/list
📜 병원 규칙 관리    → /admin/rule/list
👤 내 정보관리       → /admin/mypage
```

---

## 9. 관리자 — 병원 규칙 관리 흐름 (ROLE_ADMIN)

### 🎯 목적
챗봇이 참조하는 병원 규칙 문서를 등록·수정·삭제한다. 변경 즉시 챗봇에 반영된다.

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant A  as Admin (ROLE_ADMIN)
    participant W  as Web Server
    participant DB as Database

    A->>W: GET /admin/rule/list
    W->>DB: HospitalRule 전체 조회 (is_active 포함)
    DB-->>W: 규칙 목록 반환
    W-->>A: 규칙 목록 렌더링 (admin/rule/list)

    alt 규칙 등록
        A->>W: GET /admin/rule/new
        W-->>A: 등록 폼 렌더링 (admin/rule/new) — categories 목록 포함

        A->>W: POST /admin/rule/create
        Note over A,W: {title, content, category}
        W->>DB: HospitalRule 저장 (is_active=TRUE)

        alt 저장 성공
            W-->>A: redirect:/admin/rule/list [PRG]
            Note over W,A: Flash: successMessage = "규칙이 등록되었습니다. 챗봇에 즉시 반영됩니다."
        else 유효성 오류
            W-->>A: 폼 재렌더링 (admin/rule/new)
            Note over W,A: request.setAttribute: errorCode / inputData 복원
        end
    end

    alt 규칙 수정
        A->>W: GET /admin/rule/detail?ruleId=1
        W->>DB: HospitalRule 조회
        DB-->>W: 규칙 데이터 반환
        W-->>A: 상세·수정 폼 렌더링 (admin/rule/detail)

        A->>W: POST /admin/rule/update
        Note over A,W: {ruleId, title, content, category}
        W->>DB: HospitalRule 업데이트
        W-->>A: redirect:/admin/rule/detail?ruleId=1 [PRG]
        Note over W,A: Flash: successMessage = "규칙이 수정되었습니다. 챗봇에 즉시 반영됩니다."
    end

    alt 활성화 상태 토글
        A->>W: POST /admin/rule/toggleActive
        Note over A,W: {ruleId:1}
        W->>DB: is_active 반전 (TRUE↔FALSE)
        W-->>A: redirect:/admin/rule/list [PRG]
        Note over W,A: Flash: successMessage (활성화/비활성화 구분 메시지)
    end

    alt 규칙 삭제
        A->>W: POST /admin/rule/delete
        Note over A,W: {ruleId:1}
        W->>DB: HospitalRule 삭제
        W-->>A: redirect:/admin/rule/list [PRG]
        Note over W,A: Flash: successMessage = "규칙이 삭제되었습니다."
    end
```

### 📝 흐름 설명

1. **등록·수정 화면 분리** : 등록은 `/admin/rule/new`, 상세·수정은 `/admin/rule/detail`. 등록 폼과 수정 폼이 별도 화면.
2. **즉시 반영** : 수정·추가 후 다음 챗봇 요청부터 바로 반영. 별도 캐시 무효화 불필요 (매 요청마다 DB 조회).
3. **비활성화** : `is_active = FALSE` → 챗봇 프롬프트에서 제외. 규칙 데이터는 보존.
4. **전체 PRG** : 모든 POST 처리 후 redirect. 새로고침 중복 제출 방지.

---

## 10. 관리자 — 인사 관리 흐름 (ROLE_ADMIN)

### 🎯 목적
직원·의사 등록을 관리한다.

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant A  as Admin (ROLE_ADMIN)
    participant W  as Web Server
    participant DB as Database

    Note over A,DB: ─── 직원 관리 ───

    A->>W: GET /admin/staff/list
    W->>DB: Staff 전체 조회 (ROLE·isActive 필터 가능)
    DB-->>W: 직원 목록 반환
    W-->>A: 직원 목록 렌더링 (admin/staff/list)

    alt 직원 등록
        A->>W: GET /admin/staff/new
        W->>DB: 활성 진료과 목록 조회
        DB-->>W: 진료과 목록 반환
        W-->>A: 등록 폼 렌더링 (admin/staff/new)

        A->>W: POST /admin/staff/create
        Note over A,W: {username, password, name, role, departmentId, availableDays, specialty}
        W->>DB: username 중복 체크
        DB-->>W: 중복 여부 반환

        alt 등록 가능
            Note over W,DB: @Transactional 시작
            W->>DB: Staff 저장 (BCrypt 암호화)
            alt ROLE_DOCTOR인 경우
                W->>DB: Doctor 저장 (staff_id, department_id, available_days, specialty)
            end
            Note over W,DB: @Transactional 커밋
            W-->>A: redirect:/admin/staff/list [PRG]
            Note over W,A: Flash: successMessage = "직원이 등록되었습니다."
        else username 중복
            W-->>A: 폼 재렌더링 (admin/staff/new)
            Note over W,A: request.setAttribute: errorCode=DUPLICATE_USERNAME / inputData
        end
    end

    alt 직원 수정
        A->>W: GET /admin/staff/detail?staffId=16
        W->>DB: Staff 상세 + 진료과 목록 조회
        DB-->>W: 직원 정보 + 진료과 목록 반환
        W-->>A: 상세·수정 폼 렌더링 (admin/staff/detail)

        A->>W: POST /admin/staff/update
        Note over A,W: {staffId, name, departmentId, availableDays, specialty}
        W->>DB: Staff + Doctor 정보 업데이트 (트랜잭션)
        W-->>A: redirect:/admin/staff/detail?staffId=16 [PRG]
        Note over W,A: Flash: successMessage = "직원 정보가 수정되었습니다."
    end

    alt 직원 비활성화 (퇴직 처리)
        A->>W: POST /admin/staff/deactivate
        Note over A,W: {staffId:16}
        W->>DB: Staff.is_active = FALSE
        W-->>A: redirect:/admin/staff/list [PRG]
        Note over W,A: Flash: successMessage (비활성 직원은 로그인 차단)
    end

```

### 📝 흐름 설명

1. **직원 등록 트랜잭션** : `ROLE_DOCTOR`인 경우 `Staff` + `Doctor` 레코드를 `@Transactional`로 동시 저장.
2. **퇴직 처리** : 삭제 대신 `is_active = FALSE`. 기존 예약 이력 보존. 비활성 직원은 Spring Security 레벨에서 로그인 차단.

---

## 11. 물품 관리자 — 물품 관리 흐름 (ROLE_ITEM_MANAGER)

### 🎯 목적
물품 재고를 관리한다. 물품 목록 조회, 등록, 수정을 담당한다.

### 🔄 시퀀스 다이어그램

```mermaid
sequenceDiagram
    participant IM as ItemManager (ROLE_ITEM_MANAGER)
    participant W  as Web Server
    participant DB as Database

    IM->>W: POST /login (username, password)
    W->>DB: Staff 인증 (ROLE_ITEM_MANAGER 확인)
    DB-->>W: ROLE_ITEM_MANAGER 세션 발급
    W-->>IM: redirect:/item-manager/item/list [Spring Security]

    IM->>W: GET /item-manager/item/list
    W->>DB: Item 전체 조회 (재고 부족 강조용 isShortage 포함)
    DB-->>W: 물품 목록 반환
    W-->>IM: 물품 목록 렌더링 (item-manager/item/list)

    alt 물품 등록
        IM->>W: GET /item-manager/item/new
        W-->>IM: 등록 폼 렌더링 (item-manager/item/new) — categories 목록 포함

        IM->>W: POST /item-manager/item/create
        Note over IM,W: {name, category, quantity, minQuantity}
        W->>DB: Item 저장
        W-->>IM: redirect:/item-manager/item/list [PRG]
        Note over W,IM: Flash: successMessage = "물품이 등록되었습니다."
    end

    alt 물품 수정
        IM->>W: GET /item-manager/item/detail?itemId=1
        W->>DB: Item 상세 조회
        DB-->>W: 물품 정보 반환
        W-->>IM: 상세·수정 폼 렌더링 (item-manager/item/detail)

        IM->>W: POST /item-manager/item/update
        Note over IM,W: {itemId, name, category, quantity, minQuantity}
        W->>DB: Item 전체 필드 업데이트
        W-->>IM: redirect:/item-manager/item/detail?itemId=1 [PRG]
        Note over W,IM: Flash: successMessage = "물품 정보가 수정되었습니다."
    end
```

### 📝 흐름 설명

1. **물품 등록·수정 화면 분리** : `/item-manager/item/new` (등록), `/item-manager/item/detail` (수정).
2. **물품 update** : 전체 필드(이름, 카테고리, 수량, 최소 수량)를 수정 가능. API spec v5.0과 동기화.
3. **PRG 패턴** : 등록·수정 후 목록 또는 상세 화면으로 redirect + Flash Attribute.

### 📋 사이드바 메뉴 (ROLE_ITEM_MANAGER)

```
📦 물품 목록        → /item-manager/item/list
👤 내 정보관리       → /item-manager/mypage
```

---

## 12. 내 정보관리 흐름 (전 역할)

### 🎯 목적
로그인된 직원(STAFF, DOCTOR, NURSE, ADMIN)이 자신의 이메일·연락처를 수정하고 비밀번호를 변경한다. 이름, 사원번호 등 기본 인사정보는 읽기 전용으로 표시한다.

---

### 12-1. STAFF 내 정보관리 흐름

```mermaid
sequenceDiagram
    participant S  as Staff (ROLE_STAFF)
    participant W  as Web Server
    participant DB as Database

    S->>W: GET /staff/mypage
    W->>DB: Staff 본인 정보 조회 (session → staffId)
    DB-->>W: 직원 정보 반환
    W-->>S: 내 정보관리 화면 렌더링 (staff/mypage)
    Note over S: 기본 정보 표시 (이름, 사원번호, 담당업무 — 읽기전용)
    Note over S: 이메일·연락처 수정 가능 / 비밀번호 변경 가능

    S->>W: POST /staff/mypage/update
    Note over S,W: {email, phone, currentPassword?, newPassword?}
    W->>DB: Staff 정보 업데이트 (비밀번호 변경 시 BCrypt 재암호화)
    W-->>S: redirect:/staff/mypage [PRG]
    Note over W,S: Flash: successMessage = "내 정보가 수정되었습니다."
    S->>W: GET /staff/mypage
    W-->>S: 화면 렌더링 (successMessage 표시)
```

**화면 구성:**
```
GET /staff/mypage → staff/mypage.mustache
├── 기본 정보 표시 (이름, 사원번호, 담당업무 — 읽기전용)
├── 이메일·연락처 수정
├── 비밀번호 변경
└── POST /staff/mypage/update → redirect:/staff/mypage [PRG]
```

---

### 12-2. DOCTOR 내 정보관리 흐름

```mermaid
sequenceDiagram
    participant D  as Doctor (ROLE_DOCTOR)
    participant W  as Web Server
    participant DB as Database

    D->>W: GET /doctor/mypage
    W->>DB: Staff + Doctor 본인 정보 조회 (session → staffId)
    DB-->>W: 직원 + 의사 정보 반환
    W-->>D: 내 정보관리 화면 렌더링 (doctor/mypage)
    Note over D: 기본 정보 표시 (이름, 사원번호, 전문과 — 읽기전용)
    Note over D: 이메일·연락처 수정 가능 / 비밀번호 변경 가능

    D->>W: POST /doctor/mypage/update
    Note over D,W: {email, phone, currentPassword?, newPassword?}
    W->>DB: Staff 정보 업데이트 (비밀번호 변경 시 BCrypt 재암호화)
    W-->>D: redirect:/doctor/mypage [PRG]
    Note over W,D: Flash: successMessage = "내 정보가 수정되었습니다."
    D->>W: GET /doctor/mypage
    W-->>D: 화면 렌더링 (successMessage 표시)
```

**화면 구성:**
```
GET /doctor/mypage → doctor/mypage.mustache
├── 기본 정보 표시 (이름, 사원번호, 전문과 — 읽기전용)
├── 이메일·연락처 수정
├── 비밀번호 변경
└── POST /doctor/mypage/update → redirect:/doctor/mypage [PRG]
```

---

### 12-3. NURSE 내 정보관리 흐름

```mermaid
sequenceDiagram
    participant N  as Nurse (ROLE_NURSE)
    participant W  as Web Server
    participant DB as Database

    N->>W: GET /nurse/mypage
    W->>DB: Staff 본인 정보 조회 (session → staffId)
    DB-->>W: 직원 정보 반환
    W-->>N: 내 정보관리 화면 렌더링 (nurse/mypage)
    Note over N: 기본 정보 표시 (이름, 사원번호, 부서 — 읽기전용)
    Note over N: 이메일·연락처 수정 가능 / 비밀번호 변경 가능

    N->>W: POST /nurse/mypage/update
    Note over N,W: {email, phone, currentPassword?, newPassword?}
    W->>DB: Staff 정보 업데이트 (비밀번호 변경 시 BCrypt 재암호화)
    W-->>N: redirect:/nurse/mypage [PRG]
    Note over W,N: Flash: successMessage = "내 정보가 수정되었습니다."
    N->>W: GET /nurse/mypage
    W-->>N: 화면 렌더링 (successMessage 표시)
```

**화면 구성:**
```
GET /nurse/mypage → nurse/mypage.mustache
├── 기본 정보 표시 (이름, 사원번호, 부서 — 읽기전용)
├── 이메일·연락처 수정
├── 비밀번호 변경
└── POST /nurse/mypage/update → redirect:/nurse/mypage [PRG]
```

---

### 12-4. ADMIN 내 정보관리 흐름

```mermaid
sequenceDiagram
    participant A  as Admin (ROLE_ADMIN)
    participant W  as Web Server
    participant DB as Database

    A->>W: GET /admin/mypage
    W->>DB: Staff 본인 정보 조회 (session → staffId)
    DB-->>W: 직원 정보 반환
    W-->>A: 내 정보관리 화면 렌더링 (admin/mypage)
    Note over A: 기본 정보 표시 (이름, 사원번호, 담당업무 — 읽기전용)
    Note over A: 이메일·연락처 수정 가능 / 비밀번호 변경 가능

    A->>W: POST /admin/mypage/update
    Note over A,W: {email, phone, currentPassword?, newPassword?}
    W->>DB: Staff 정보 업데이트 (비밀번호 변경 시 BCrypt 재암호화)
    W-->>A: redirect:/admin/mypage [PRG]
    Note over W,A: Flash: successMessage = "내 정보가 수정되었습니다."
    A->>W: GET /admin/mypage
    W-->>A: 화면 렌더링 (successMessage 표시)
```

**화면 구성:**
```
GET /admin/mypage → admin/mypage.mustache
├── 기본 정보 표시 (이름, 사원번호, 담당업무 — 읽기전용)
├── 이메일·연락처 수정
├── 비밀번호 변경
└── POST /admin/mypage/update → redirect:/admin/mypage [PRG]
```

### 📝 흐름 설명 (공통)

1. **읽기전용 정보** : 이름, 사원번호, 역할별 고유 정보(담당업무/전문과/부서)는 관리자만 수정 가능하므로 읽기전용 표시.
2. **수정 가능 항목** : 이메일, 연락처는 본인이 직접 수정. 비밀번호 변경 시 현재 비밀번호 확인 후 BCrypt 재암호화.
3. **PRG 패턴** : 수정 완료 후 동일 mypage 화면으로 redirect + Flash 성공 메시지.
4. **권한 격리** : 각 역할은 자신의 URL prefix 하위(`/staff/mypage`, `/doctor/mypage` 등)에서만 접근 가능. Spring Security로 역할별 URL 접근 제어.

---

## 13. 전체 상태 흐름 다이어그램

### 🎯 목적
예약 생성부터 완료/취소까지 전체 생명주기와 각 전환의 담당 주체를 한눈에 파악한다.

### 🔄 상태 다이어그램

```mermaid
stateDiagram-v2
    [*] --> RESERVED : 온라인 예약 생성\n(비회원 직접 선택 / LLM 추천 확정)\n source=ONLINE

    [*] --> RESERVED : 전화 예약 등록\n(ROLE_STAFF)\n source=PHONE

    [*] --> RECEIVED : 방문 접수 직행\n(ROLE_STAFF)\n source=WALKIN

    RESERVED  --> RECEIVED  : 접수 처리\n(ROLE_STAFF)
    RECEIVED  --> COMPLETED : 진료 완료\n(ROLE_DOCTOR)

    RESERVED  --> CANCELLED : 취소\n(ROLE_ADMIN)
    RECEIVED  --> CANCELLED : 미방문 취소\n(ROLE_ADMIN)

    COMPLETED --> [*] : 최종 종료
    CANCELLED --> [*] : 최종 종료
```

### 📝 상태별 전환 규칙

| 현재 상태 | 다음 상태 | 전환 주체 | 역방향 |
|-----------|-----------|-----------|---------|
| `[시작]` | `RESERVED` | 비회원, ROLE_STAFF | — |
| `[시작]` | `RECEIVED` | ROLE_STAFF (방문 접수만) | — |
| `RESERVED` | `RECEIVED` | `ROLE_STAFF` | ❌ |
| `RESERVED` | `CANCELLED` | `ROLE_ADMIN` | ❌ |
| `RECEIVED` | `COMPLETED` | `ROLE_DOCTOR` | ❌ |
| `RECEIVED` | `CANCELLED` | `ROLE_ADMIN` | ❌ |
| `COMPLETED` | 없음 | — | ❌ 최종 |
| `CANCELLED` | 없음 | — | ❌ 최종 |

> ⚠️ **Service 레이어 검증 필수:** 상태 전이는 화면 버튼 제어만으로 막지 않고, 반드시 Service 레이어에서 이전 상태를 검증한다. 잘못된 상태 변경 시 `INVALID_STATUS_TRANSITION` 예외 발생.

---

## 14. 권한 기반 화면 흐름 요약

### 🗺️ 역할별 전체 화면 진입 경로 (v5.0 URL 기준)

```
[외부 — 비회원 환자]
└── GET /                           비회원 메인 (진료과 안내)
└── GET /reservation                예약 방식 선택
    ├── GET /reservation/symptom    증상 입력
    │   └── POST /llm/symptom/analyze  [AJAX] LLM 추천
    │       └── GET /reservation/direct?recommended...  예약 폼 (추천값 자동 입력)
    │           └── POST /reservation/create → redirect:/reservation/complete
    └── GET /reservation/direct     직접 선택 예약 폼
        └── POST /reservation/create → redirect:/reservation/complete

[ROLE_STAFF — 접수 직원]
└── POST /login → redirect:/staff/dashboard
    └── GET /staff/dashboard            대시보드
    └── GET /staff/reception/list       오늘 접수 목록
        └── GET /staff/reception/detail  접수 처리 폼
            └── POST /staff/reception/receive → redirect:/staff/reception/list
    └── GET /staff/reservation/new      전화 예약 등록 폼 (source=PHONE)
        └── POST /staff/reservation/create → redirect:/staff/reception/list
    └── GET /staff/walkin/new           방문 접수 폼 (source=WALKIN)
        └── POST /staff/walkin/create → redirect:/staff/reception/list
    └── GET /staff/mypage               내 정보관리
        └── POST /staff/mypage/update → redirect:/staff/mypage

[ROLE_DOCTOR — 의사]
└── POST /login → redirect:/doctor/dashboard
    └── GET /doctor/dashboard           대시보드
    └── GET /doctor/treatment/list      오늘 진료 목록
        └── GET /doctor/treatment/detail  진료 기록 입력 폼
            └── POST /doctor/treatment/complete → redirect:/doctor/treatment/list
    └── [💬] POST /llm/rules/ask  [AJAX]   병원 규칙 챗봇
    └── [💬] GET /llm/rules/history  [AJAX] 챗봇 이력 조회
    └── GET /doctor/mypage              내 정보관리
        └── POST /doctor/mypage/update → redirect:/doctor/mypage

[ROLE_NURSE — 간호사]
└── POST /login → redirect:/nurse/dashboard
    └── GET /nurse/dashboard            대시보드 (재고 부족 알림 포함)
    └── GET /nurse/schedule/list        오늘 예약·접수 현황
    └── GET /nurse/patient/detail       환자 정보 조회·수정 폼
        └── POST /nurse/patient/update → redirect:/nurse/patient/detail
    └── [💬] POST /llm/rules/ask  [AJAX]   병원 규칙 챗봇
    └── [💬] GET /llm/rules/history  [AJAX] 챗봇 이력 조회
    └── GET /nurse/mypage               내 정보관리
        └── POST /nurse/mypage/update → redirect:/nurse/mypage

[ROLE_ADMIN — 관리자]
└── POST /login → redirect:/admin/dashboard
    └── GET /admin/dashboard            통계 대시보드
    └── GET /admin/reception/list       전체 접수 목록 (ONLINE/PHONE/WALKIN source별 필터링)
    └── GET /admin/reservation/list     전체 예약 조회 (source별 필터링 가능)
        └── POST /admin/reservation/cancel → redirect:/admin/reservation/list
    └── GET /admin/patient/list         환자 목록
        └── GET /admin/patient/detail   환자 상세·이력
    └── GET /admin/staff/list           직원 목록
        └── GET /admin/staff/new → POST /admin/staff/create
        └── GET /admin/staff/detail → POST /admin/staff/update
        └── POST /admin/staff/deactivate
    └── GET /admin/department/list      진료과 목록
        └── POST /admin/department/create (인라인 폼)
        └── GET /admin/department/detail → POST /admin/department/update
        └── POST /admin/department/deactivate / activate
    └── GET /admin/rule/list            병원 규칙 목록
        └── GET /admin/rule/new → POST /admin/rule/create
        └── GET /admin/rule/detail → POST /admin/rule/update
        └── POST /admin/rule/toggleActive / delete
    └── GET /admin/mypage               내 정보관리
        └── POST /admin/mypage/update → redirect:/admin/mypage

[ROLE_ITEM_MANAGER — 물품 관리자]
└── POST /login → redirect:/item-manager/item/list
    └── GET /item-manager/item/list        물품 목록
        └── GET /item-manager/item/new → POST /item-manager/item/create
        └── GET /item-manager/item/detail → POST /item-manager/item/update
    └── GET /item-manager/mypage           내 정보관리
        └── POST /item-manager/mypage/update → redirect:/item-manager/mypage
```

### 📊 권한별 기능 접근 매트릭스

| 기능 | 비회원 | STAFF | DOCTOR | NURSE | ADMIN | ITEM_MANAGER |
|------|:------:|:-----:|:------:|:-----:|:-----:|:------------:|
| LLM 증상 추천 예약 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 직접 선택 예약 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 역할별 대시보드 | ❌ | ✅ | ✅ | ✅ | ✅ | ❌ |
| 오늘 예약 목록 조회 | ❌ | ✅ | ✅ 본인만 | ✅ | ✅ | ❌ |
| 접수 처리 (RESERVED→RECEIVED) | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ |
| **방문 접수 (즉시 RECEIVED)** | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 전화 예약 등록 | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 진료 기록 입력 (RECEIVED→COMPLETED) | ❌ | ❌ | ✅ 본인만 | ❌ | ❌ | ❌ |
| 예약 취소 | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| 환자 정보 수정 | ❌ | ✅ 접수 시 | ❌ | ✅ | ✅ | ❌ |
| **내 정보관리 (mypage)** | ❌ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **병원 규칙 Q&A 챗봇** | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| **챗봇 이력 조회** | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| 전체 예약 조회 (전 기간) | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| **전체 접수 현황 (source별 필터링: ONLINE/PHONE/WALKIN)** | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| 환자 목록·이력 조회 | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| 직원 CRUD | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| 진료과 CRUD | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| 물품 CRUD | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| **병원 규칙 CRUD** | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| 통계 대시보드 | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |

### 📋 URL별 권한 매트릭스 (내 정보관리)

| URL | Method | 필요 권한 | 설명 |
|-----|--------|-----------|------|
| `/staff/mypage` | GET | ROLE_STAFF | 내 정보관리 화면 |
| `/staff/mypage/update` | POST | ROLE_STAFF | 내 정보 수정 |
| `/doctor/mypage` | GET | ROLE_DOCTOR | 내 정보관리 화면 |
| `/doctor/mypage/update` | POST | ROLE_DOCTOR | 내 정보 수정 |
| `/nurse/mypage` | GET | ROLE_NURSE | 내 정보관리 화면 |
| `/nurse/mypage/update` | POST | ROLE_NURSE | 내 정보 수정 |
| `/admin/mypage` | GET | ROLE_ADMIN | 내 정보관리 화면 |
| `/admin/mypage/update` | POST | ROLE_ADMIN | 내 정보 수정 |
| `/item-manager/mypage` | GET | ROLE_ITEM_MANAGER | 내 정보관리 화면 |
| `/item-manager/mypage/update` | POST | ROLE_ITEM_MANAGER | 내 정보 수정 |

---

## 15. SSR PRG 패턴 상세

### 15.1 PRG 패턴 적용 원칙

v5.0까지 모든 `POST` 처리는 **Post-Redirect-Get (PRG)** 패턴을 따른다.

```
[브라우저] POST /staff/reception/receive
    ↓ 성공
[서버] 302 redirect:/staff/reception/list  (Flash Attribute 첨부)
    ↓
[브라우저] GET /staff/reception/list  (Flash 소비)
    ↓
[서버] return "staff/reception/list"  (뷰 렌더링)
```

**PRG 패턴의 이점:**
- 브라우저 새로고침 시 POST 재전송 방지
- 뒤로 가기 시 폼 재전송 경고창 미노출
- URL이 항상 GET 화면 경로를 가리켜 북마크·공유 가능

### 15.2 Flash Attribute 전달 방식

```java
// POST 컨트롤러 (성공 시)
@PostMapping("/staff/reception/receive")
public String receive(ReceiveRequest req, RedirectAttributes ra) {
    service.receive(req);
    ra.addFlashAttribute("successMessage", "접수가 완료되었습니다.");
    return "redirect:/staff/reception/list";
}

// GET 컨트롤러 (목록 화면 — Flash 자동 수신)
@GetMapping("/staff/reception/list")
public String list(HttpServletRequest request) {
    // successMessage는 Model에 자동 바인딩됨
    // Mustache 템플릿에서 {{#successMessage}} ... {{/successMessage}} 로 표시
    request.setAttribute("reservations", service.getTodayList());
    return "staff/reception/list";
}
```

### 15.3 오류 시 폼 재렌더링 패턴

```java
// POST 컨트롤러 (오류 시 — 폼 재렌더링)
@PostMapping("/staff/reception/receive")
public String receive(ReceiveRequest req, HttpServletRequest request) {
    try {
        service.receive(req);
        // ...redirect
    } catch (InvalidStatusTransitionException e) {
        request.setAttribute("errorCode", "INVALID_STATUS_TRANSITION");
        request.setAttribute("errorMessage", "RESERVED 상태에서만 접수 가능합니다.");
        request.setAttribute("reservation", service.getDetail(req.getReservationId()));
        request.setAttribute("patient", service.getPatient(req.getReservationId()));
        return "staff/reception/detail";  // 폼 뷰 재반환 (inputData 복원)
    }
}
```

### 15.4 AJAX 엔드포인트 구분

아래 엔드포인트는 비동기 호출이므로 `@ResponseBody` JSON 응답 유지:

| URL | 용도 |
|-----|------|
| `POST /llm/symptom/analyze` | LLM 증상 분석 |
| `POST /llm/rules/ask` | LLM 규칙 챗봇 |
| `GET /llm/rules/history` | 챗봇 이력 조회 |
| `GET /reservation/getDoctors` | 의사 목록 동적 갱신 |
| `GET /reservation/getSlots` | 시간 슬롯 동적 갱신 |
| `GET /admin/dashboard/stats` | 대시보드 통계 갱신 |

---

## 16. LLM 연동 흐름 상세

### 16.1 증상 추천 vs 규칙 Q&A 비교

| 항목 | 증상 추천 | 규칙 Q&A |
|------|-----------|----------|
| 사용 대상 | 외부 비회원 환자 | ROLE_DOCTOR, ROLE_NURSE |
| 입력 | 증상 텍스트 | 질문 텍스트 |
| 프롬프트 소스 | DB: DEPARTMENT + DOCTOR | DB: HOSPITAL_RULE (is_active=TRUE) |
| 응답 형식 | JSON (파싱 후 추천 화면) | 자연어 텍스트 |
| 이력 저장 | ✅ LLM_RECOMMENDATION | ✅ CHATBOT_HISTORY (session_id, staff_id, question, answer, created_at) |
| 폴백 처리 | 직접 선택 화면 전환 | "서비스 불가" 안내 |
| 면책 고지 | ✅ 화면 필수 표시 | 규칙 범위 안내 |
| 타임아웃 | 5초 | 5초 |

### 16.2 LlmService 공통 흐름

```mermaid
sequenceDiagram
    participant SVC as LlmService
    participant DB  as Database
    participant C   as Claude API

    SVC->>DB: 프롬프트 소스 조회
    Note over SVC,DB: 증상 추천: DEPARTMENT + DOCTOR\n규칙 Q&A: HOSPITAL_RULE (is_active=TRUE)
    DB-->>SVC: 데이터 반환

    SVC->>SVC: 시스템 프롬프트 구성 (DB 데이터 문자열 주입)

    SVC->>C: POST /v1/messages
    Note over SVC,C: model: claude-sonnet-4-6\nAPI Key: 서버 환경변수 (CLAUDE_API_KEY)\ntimeout: 5s

    alt 정상 응답 (200 OK)
        C-->>SVC: 응답 반환
        SVC->>SVC: 응답 파싱 → DTO 또는 텍스트 반환
    else 타임아웃 (5초 초과)
        C-->>SVC: 타임아웃 예외
        SVC-->>SVC: 폴백 신호 반환
    else API 오류 (4xx / 5xx)
        C-->>SVC: 오류 응답
        SVC-->>SVC: 폴백 신호 반환
    end
```

### 16.3 API Key 보안 원칙

```
브라우저 → Spring Boot 서버 → Claude API

규칙:
  ✅ API Key는 서버 환경변수로만 관리
  ✅ LlmService 내부에서만 Claude API 호출
  ❌ 브라우저에서 Claude API 직접 호출 금지
  ❌ application.properties에 API Key 하드코딩 금지
  ❌ Git 저장소에 API Key 커밋 금지 (.gitignore 등록 필수)

설정:
  # application.properties
  claude.api.key=${CLAUDE_API_KEY}

  # 서버 환경변수 설정 (Linux)
  export CLAUDE_API_KEY=sk-ant-xxxx
```

---

## 17. v4.0 → v5.0 변경 내역

### 📋 변경 요약

| 항목 | v4.0 | v5.0 | 비고 |
|------|------|------|------|
| **내 정보관리 흐름** | 없음 | 전 역할(STAFF, DOCTOR, NURSE, ADMIN) mypage 추가 | 스토리보드 정합 |
| **RESERVED → RECEIVED 전환 주체** | ROLE_STAFF, ROLE_ADMIN | ROLE_STAFF | 스토리보드 기준 통일 |
| **RECEIVED → COMPLETED 전환 주체** | ROLE_DOCTOR, ROLE_ADMIN | ROLE_DOCTOR | 스토리보드 기준 통일 |
| **→ CANCELLED 전환 주체** | ROLE_STAFF / ROLE_ADMIN | ROLE_ADMIN | 스토리보드 기준 통일 |
| **API 명세서 연동 버전** | v4.0 | v5.0 | 동기화 |
| **ERD 연동 버전** | v3.0 | v4.0 | 동기화 |

### 📝 상세 변경 사항

1. **내 정보관리 흐름 신규 (섹션 11)**
   - STAFF: `GET /staff/mypage`, `POST /staff/mypage/update`
   - DOCTOR: `GET /doctor/mypage`, `POST /doctor/mypage/update`
   - NURSE: `GET /nurse/mypage`, `POST /nurse/mypage/update`
   - ADMIN: `GET /admin/mypage`, `POST /admin/mypage/update`
   - 공통: 기본 정보 읽기전용 표시, 이메일·연락처 수정, 비밀번호 변경, PRG 패턴 적용

2. **예약 상태 전이 권한 변경 (스토리보드 기준 통일)**
   - 접수 처리(RESERVED → RECEIVED): ROLE_STAFF 단독 (ROLE_ADMIN 제외)
   - 진료 완료(RECEIVED → COMPLETED): ROLE_DOCTOR 단독 (ROLE_ADMIN 제외)
   - 취소(→ CANCELLED): ROLE_ADMIN 단독 (ROLE_STAFF 제외)
   - 상태 다이어그램, 전환 규칙 테이블, 권한 매트릭스 전면 동기화

3. **사이드바 메뉴 갱신**
   - 전 역할 사이드바에 "👤 내 정보관리" 메뉴 항목 추가

4. **URL 트리 갱신**
   - 전 역할 URL 트리에 mypage / mypage/update 경로 추가

5. **권한 매트릭스 갱신**
   - 기능 접근 매트릭스에 "내 정보관리 (mypage)" 행 추가
   - URL별 권한 매트릭스(내 정보관리) 신규 추가
   - 접수 처리·진료 완료·예약 취소 권한 칸 스토리보드 기준 수정

6. **연관 문서 참조 갱신**
   - ERD: v3.0 → v4.0
   - API 명세서: v4.0 → v5.0

---

## 📌 설계 시 주요 고려 사항

**PRG 패턴 전면 적용**
모든 `POST` 처리 성공 후 반드시 `redirect:`를 반환한다. 새로고침 중복 제출 방지가 목적이다. 오류 시에만 폼 재렌더링을 사용한다.

**상태 전이 검증 위치**
상태 변경은 화면 버튼 제어만으로 막지 않고, 반드시 Service 레이어에서 이전 상태를 검증한다. 잘못된 전이 시도 시 `INVALID_STATUS_TRANSITION` 예외 발생.

**상태 전이 권한 분리 (v5.0 변경)**
스토리보드 기준으로 접수(ROLE_STAFF), 진료 완료(ROLE_DOCTOR), 취소(ROLE_ADMIN)를 명확히 분리한다. 각 역할이 자신의 업무 범위 내에서만 상태를 변경하여 책임 소재를 명확히 한다.

**중복 예약 이중 방어**
Service 레이어에서 사전 조회로 명확한 오류 메시지를 반환하고, DB UNIQUE 제약 `(doctor_id, reservation_date, time_slot)`이 최후 방어선을 담당한다.

**방문 접수 트랜잭션**
`Patient` 저장 + `Reservation` 저장을 반드시 `@Transactional`로 묶는다. Patient 저장 후 Reservation 저장 실패 시 Patient도 롤백.

**TreatmentRecord + Reservation 트랜잭션**
진료 완료 처리 시 `TreatmentRecord` 저장 + `Reservation.status = COMPLETED` 변경은 반드시 하나의 `@Transactional` 내에서 처리한다.

**LLM 폴백 처리 필수**
증상 추천 실패 시 사용자 예약 흐름이 끊기지 않도록 직접 선택 화면으로 자연스럽게 전환한다. 규칙 챗봇 실패 시 관리자 문의 안내를 표시한다.

**LLM 면책 고지 필수**
증상 추천 결과 화면에는 `disclaimer` 문구를 반드시 표시한다. Mustache 템플릿 조건부 렌더링으로 누락 방지.

**RESERVATION.source 컬럼 활용**
모든 예약 생성 시 유입 경로를 `source` 컬럼에 기록한다. `ONLINE`(비회원 직접/LLM 추천), `PHONE`(전화 예약), `WALKIN`(방문 접수) 세 가지 값을 사용한다. 관리자 화면에서 source별 필터링을 지원한다.

**내 정보관리 보안 원칙 (v5.0 추가)**
비밀번호 변경 시 현재 비밀번호를 반드시 확인한다. 비밀번호는 BCrypt로 재암호화하여 저장한다. 이름·사원번호 등 인사 기본정보는 본인이 수정할 수 없으며 관리자 직원 관리 화면에서만 변경 가능하다.

---

*본 문서는 프로젝트 계획서 v4.2, ERD v4.0, 화면 정의서 v2.0, API 명세서 v5.0, 스토리보드를 기반으로 작성되었습니다.*
*변경 발생 시 GitHub Wiki에서 버전 이력을 관리합니다.*
